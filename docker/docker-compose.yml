version: '3'

services:
  mongo:
    image: mongo:latest
    restart: always
    ports:
      - '27013:27017'
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: HelloThere
    volumes:
      - '../dumps:/db_dump'
